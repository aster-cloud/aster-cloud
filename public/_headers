# Cloudflare Workers 静态资源自定义响应头（与 Pages `_headers` 同语法）。
# OpenNext 构建时把 public/ 逐一拷进 .open-next/assets/（wrangler.toml 的 [assets]
# directory 指向它），本文件随之进 assets 根，由 Workers Static Assets 原生识别。
# 文档：https://developers.cloudflare.com/workers/static-assets/headers/
#
# 目的：让较大的自托管音频（/audio/*.mp3，约 4 MB）走 Cloudflare 边缘缓存，
# 避免每次播放都回源、增加 Worker/源站压力。
#
# 缓存策略：max-age=31536000（一年）+ immutable——音频是稳定内容，浏览器与
# 边缘都长期缓存，命中后不回源、不再校验。
# ★内容失效靠**版本化文件名**（当前 guyong-v1.mp3）：若日后替换录音内容，必须换 URL
#   （改 guyong-v2.mp3 并更新 <audio src>）。
#   ——不能靠 Cloudflare purge：purge 只清 CDN 边缘，清不掉已在用户浏览器里 fresh 一年且
#     带 immutable 的旧副本；只有换 URL 才能让浏览器与边缘同时可靠失效。
/audio/*
  Cache-Control: public, max-age=31536000, immutable
