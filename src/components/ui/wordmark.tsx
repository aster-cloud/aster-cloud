/**
 * Re-export of the Wordmark from the design system.
 *
 * aster-cloud uses the `product` variant in the dashboard navbar header
 * and the default variant in the marketing footer.
 */
export { Wordmark, type WordmarkProps } from '@aster-cloud/ui';
